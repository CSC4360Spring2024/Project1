import 'package:flutter/material.dart';

class ReportsAndChartsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reports and Charts'),
      ),
      body: Center(
        child: Text('Reports and Charts Screen'),
      ),
    );
  }
}
