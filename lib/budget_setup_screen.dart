import 'package:flutter/material.dart';

class BudgetSetupScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Budget Setup'),
      ),
      body: Center(
        child: Text('Budget Setup Screen'),
      ),
    );
  }
}
